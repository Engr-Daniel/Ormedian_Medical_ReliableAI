# Lightweight results

Keep small derived tables and concise result summaries here, such as CSV or Markdown. Include the experiment ID in the filename or accompanying summary and link the corresponding [execution record](../experiments/README.md).

Document column meanings, units, relevant configuration, and how the result can be regenerated. Distinguish observed results from illustrative examples. Interpretations should state limitations rather than treating an isolated number as evidence of generalization.

These files are eligible for Git; this directory is not ignored. Inspect file size and sensitivity before adding outputs. Large arrays, raw predictions, datasets, and model weights belong outside Git. No results have been generated yet.
